<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="GradeBenefit">
	<select id="selectBenefitsByGradeId" parameterType="int" resultType="GradeBenefit">
		select benefit_id as benefitId, grade_id as gradeId, title, summary, detail from grade_benefit
    	where grade_id = #{value} order by benefit_id asc
	</select>
</mapper>